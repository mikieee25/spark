export default function ActivityPage() {
  return <section><p className="text-xs font-bold uppercase tracking-[0.2em] text-[var(--pulse)]">Activity</p><h1 className="mt-2 text-3xl font-black">Activity tracking arrives in Phase 2.</h1><p className="mt-3 text-[var(--ink-muted)]">Uploads, folders, edits, downloads, and recoverable deletes will be recorded with proper timestamps.</p></section>;
}
