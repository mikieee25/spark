"use client";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { adminApi, type AdminUserView } from "./admin-api";
export function UserManagement() { const [users, setUsers] = useState<AdminUserView[]>([]); const [error, setError] = useState(""); const [pending, setPending] = useState(false); useEffect(() => { void adminApi.users().then((result) => setUsers(result.users)).catch((reason: Error) => setError(reason.message)); }, []); async function create(form: HTMLFormElement) { setPending(true); setError(""); const data = new FormData(form); try { const result = await adminApi.createUser({ username: data.get("username"), displayName: data.get("displayName"), password: data.get("password") }); setUsers((current) => [...current, result.user]); form.reset(); } catch (reason) { setError(reason instanceof Error ? reason.message : "CREATE_FAILED"); } finally { setPending(false); } } async function toggle(user: AdminUserView) { try { await adminApi.updateUser({ userId: user.id, disabled: !user.disabledAt }); setUsers((current) => current.map((item) => item.id === user.id ? { ...item, disabledAt: user.disabledAt ? null : new Date().toISOString() } : item)); } catch (reason) { setError(reason instanceof Error ? reason.message : "UPDATE_FAILED"); } } return <Card><CardHeader><CardTitle>Accounts</CardTitle></CardHeader><CardContent className="grid gap-4"><form className="grid gap-2 sm:grid-cols-3" onSubmit={(event) => { event.preventDefault(); void create(event.currentTarget); }}><Input name="username" placeholder="Username" required /><Input name="displayName" placeholder="Display name" required /><Input name="password" type="password" minLength={12} placeholder="Initial password" required /><Button disabled={pending} className="sm:col-span-3">Create account</Button></form>{error && <p role="alert" className="text-sm text-destructive">{error}</p>}<div className="divide-y rounded-lg border">{users.map((user) => <div className="flex flex-wrap items-center justify-between gap-3 p-3" key={user.id}><div><p className="font-medium">{user.displayName}</p><p className="text-xs text-muted-foreground">{user.username} · {user.role}{user.disabledAt ? " · disabled" : ""}</p></div><Button variant="outline" onClick={() => void toggle(user)}>{user.disabledAt ? "Reactivate" : "Disable"}</Button></div>)}</div></CardContent></Card>; }
